<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.abc.ballot">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".result_election_list"
            android:label="Result"
            android:theme="@style/AppTheme"></activity>

        <activity android:name=".Candidate_List"
            android:label="Candidate List"
            android:theme="@style/AppTheme"/>

        <activity
            android:name=".splash"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Election_process"
            android:label="Election Process"
            android:theme="@style/AppTheme"/>

        <activity
            android:name=".Election_profile"
            android:theme="@style/AppTheme"
            android:label="  Posts  "/>

        <activity android:name=".Upload_profile"
            android:label="Upload Profile"
            android:theme="@style/AppTheme"/>

        <activity android:name=".change_password"
            android:label="Change Password"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".StartPage"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <activity
            android:name=".AdminLogin"
            android:theme="@style/AppTheme"
            android:label=" Admin Login "/>

        <activity android:name=".registration"
            android:label=" Registration "
            android:theme="@style/AppTheme"/>

        <activity android:name=".Admin_homepage"
            android:theme="@style/AppTheme"
            android:label=" Admin Homepage "/>

        <activity
            android:name=".student_homepage"
            android:label=" Conducted Election List "/>

        <activity android:name=".candidate_data"
            android:label=" Apply For Posts" />

        <activity android:name=".LoginActivity"
            android:label=" Login "/>

        <activity android:name=".AddPost"
            android:label=" Add Posts "
            android:theme="@style/AppTheme"/>

        <activity android:name=".ResultPage"
            android:label=" Result of Election "
            android:theme="@style/AppTheme" />
    </application>

</manifest>